apply plugin: 'com.android.library'

android {
    compileSdkVersion 34
    namespace 'com.chanapps.four.gallery3d'

    defaultConfig {
        minSdkVersion 15
        targetSdkVersion 22
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}
